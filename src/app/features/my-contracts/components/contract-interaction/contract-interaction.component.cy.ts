import { ContractInteractionComponent } from './contract-interaction.component'

describe('ContractInteractionComponent', () => {
  it('should mount', () => {
    cy.mount(ContractInteractionComponent)
  })
})